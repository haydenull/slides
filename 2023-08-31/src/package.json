{
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /2023/fetch-bot/ --out ../../dist/2023/fetch-bot",
    "export": "slidev export --per-slide --output ../fetch-bot.pdf"
  }
}